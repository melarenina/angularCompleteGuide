export class Ingredient{
    //This will create the variables name and amount, and automatically assign the values recieved in the
    //constructor to the variables
    constructor(public name: string, public amount: number){}
}

